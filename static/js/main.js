const canvas=document.getElementById('stars');if(canvas){const x=canvas.getContext('2d');let w,h,s=[];function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;const count=innerWidth<640?45:85;s=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.3,v:Math.random()*.18+.04}))}function draw(){x.clearRect(0,0,w,h);for(let i=0;i<s.length;i++){const a=s[i];a.y+=a.v;if(a.y>h)a.y=0;for(let j=i+1;j<s.length;j++){const b=s[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.sqrt(dx*dx+dy*dy);if(d<125){x.strokeStyle=`rgba(167,137,255,${(1-d/125)*.12})`;x.lineWidth=1;x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke()}}x.fillStyle='#d8ccff';x.globalAlpha=.2+Math.random()*.35;x.beginPath();x.arc(a.x,a.y,a.r,0,Math.PI*2);x.fill()}x.globalAlpha=1;requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw()}
const revealItems=document.querySelectorAll('.reveal-on-scroll');if(revealItems.length&&'IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.14});revealItems.forEach(item=>observer.observe(item))}else{revealItems.forEach(item=>item.classList.add('is-visible'))}
